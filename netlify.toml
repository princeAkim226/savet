[build]
  command = "npm ci && npm run build"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"
  NPM_VERSION = "9"
  # Si pnpm est utilisé (plugins Netlify), hoist pour @netlify/plugin-nextjs
  PNPM_FLAGS = "--shamefully-hoist"
